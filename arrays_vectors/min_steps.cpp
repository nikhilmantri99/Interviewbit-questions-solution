#include <bits/stdc++.h>
#include <math.h>
#include <iostream>
#include <stdlib.h>
using namespace std;

struct point{
	int x;
	int y;
};

bool compareInterval(struct point &P1, struct point &P2) 
{ 
    if(P1.x!=) 
} 


int Solution::coverPoints(vector<int> &A, vector<int> &B) {
    
}
